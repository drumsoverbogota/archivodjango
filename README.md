# archivodjango
Reescritura del archivo de pun Django

## Instalación

1. instalar requerimientos
```
pip install -r requirements.txt
```


gunicorn --bind 0.0.0.0:8000 archivodjango.wsgi:application


sudo dnf install mariadb-server

sudo systemctl enable mariadb
sudo systemctl start  mariadb


maria -u user -p

iniciar sesion

create database archivo; 
select database archivo;

use archivo;  
source archivo.sql 